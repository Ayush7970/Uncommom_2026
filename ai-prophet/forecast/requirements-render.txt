# Lightweight requirements for Render deployment
# Excludes: torch/sentence-transformers/faiss (2GB+ CUDA), streamlit, matplotlib
# FAISS analogue retrieval is skipped on Render (no persistent DB)

# Core graph + LLM
langgraph>=0.2.0
langchain>=0.3.0
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0
langsmith>=0.1.0
anthropic>=0.40.0
openai>=1.50.0

# Pydantic + config
pydantic>=2.0.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# ML + numerics (lightweight — no torch)
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.1.0

# Web search + scraping
httpx>=0.27.0
requests>=2.31.0
trafilatura>=1.6.0
tavily-python>=0.3.0

# Server
fastapi>=0.110.0
uvicorn>=0.30.0
