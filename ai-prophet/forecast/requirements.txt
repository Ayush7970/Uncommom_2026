# Core graph + LLM
langgraph>=0.2.0
langchain>=0.3.0
langchain-anthropic>=0.2.0
langchain-openai>=0.2.0
langsmith>=0.1.0
anthropic>=0.40.0
openai>=1.50.0

# Pydantic + config
pydantic>=2.0.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# ML + numerics
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.1.0
scipy>=1.12.0

# Web search + scraping
httpx>=0.27.0
requests>=2.31.0
trafilatura>=1.6.0

# Evaluation + plotting
matplotlib>=3.8.0

# Embeddings + vector search (Snowflake fallback)
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# Snowflake (optional — falls back to SQLite if not configured)
snowflake-snowpark-python>=1.18.0

# Dashboard
streamlit>=1.35.0

# Server (for --agent-url mode)
fastapi>=0.110.0
uvicorn>=0.30.0
